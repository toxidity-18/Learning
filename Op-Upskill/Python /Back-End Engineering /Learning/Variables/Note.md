# Short notes .
- A variables is always connected to a value . 
- Python always keeps track of the current value irregardless of the name of the variable 

## Naming and using variable in python . 
- Using variable in python one has to adhere to a few rules and guidelines . 
-  Variable names only contain letter . number and underscores . 
- Spaces are not around in variable names but underscores can be used as spaces .
- Always and I mean always avoid using python key words and function names as variables names . This words have they own programmatic purposes do not use them eg print . . .
- V_name should be short and descriptive.
## Avoiding naming error when using variables . . .
- All programmer make mistake but one must know how to fix this mistakes and error efficiently .
- Name error means we forgot to set a variable value or we misspelt the variable name . 
- V_name ought to be consistent with one another . 
- Many P_errors are single characters typos in one line of your program .
## Variables are labels .
- Variables act as boxes where we store value inside them . 
- Internally : variables are labels assigned to a value or used to reference certain values . 
