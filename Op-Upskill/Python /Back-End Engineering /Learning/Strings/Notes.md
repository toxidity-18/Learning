# What I have learned today 

## Strings
- Most program define and gather some sort of data and then do something useful it .
- A string is a series of character and anything inside quotes is considered to be a string . 
- This quotes can be single or double any of the above , thus allow you to use quote and apostrophes inside your string .
## Ways in which we can use string 

1. Changing the case of a string using methods 
- One simplest task you cna do with a string is to change the case of the characters inside that string. 
- A method is an action that python can perform on a piece of data .
